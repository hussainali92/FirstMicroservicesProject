apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  labels:
    app: api-gateway

spec:
  selector:
    app: api-gateway

  ports:
    - protocol: TCP
      port: 8765
      targetPort: 8765

  type: LoadBalancer