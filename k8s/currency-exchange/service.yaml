apiVersion: v1
kind: Service
metadata:
  labels:
    app: currency-exchange
  name: currency-exchange
spec:
  ports:
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: currency-exchange
  type: LoadBalancer
