apiVersion: v1
kind: Service
metadata:
  labels:
    app: currency-conversion
  name: currency-conversion
spec:
  ports:
    - protocol: TCP
      port: 8100
      targetPort: 8100
  selector:
    app: currency-conversion
  type: ClusterIP

