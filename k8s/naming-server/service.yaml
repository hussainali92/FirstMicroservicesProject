apiVersion: v1
kind: Service

metadata:
  name: naming-server
  labels:
    app: naming-server

spec:
  selector:
    app: naming-server

  ports:
    - protocol: TCP
      port: 8761
      targetPort: 8761

  type: ClusterIP